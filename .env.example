# Bot-Hosting.net / Panels
NODE_ENV=production
TZ=Africa/Yaounde

# Prefix for commands (if your bot reads settings.prefix, change in settings.js too)
PREFIX=zh

# Session directory (must be persistent on the panel!)
SESSION_DIR=./session
